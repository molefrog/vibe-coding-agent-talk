Так, это тестовый прогон презентации, я буду говорить слайд-бай-слайд.

Okay, hello everyone today I'm gonna be talking about AI coding. Well, AI coding is not something that will surprise you because you I bet everyone is using it these days. Some people.

You have probably used things like Lotus Code, Cursor or Codecs, so things that live on your machine and then can produce code. Some are more autonomous, some are integrated in the IDEs. But there's also quite a few companies that are trying to take this niche of so-called wipe coding, the coin that Andrey Karpatnyi coined, and these are basically things that don't require an in the dev environmentokay, but there is also a niche now for products that allow you to do so-called vibe coding, where you just type the prompt and you don't have to be an engineer to produce something. They don't even require you to install the dev environment. So things like Bolt, Replit, VZero, Lovable. Lovable recently crossed $6B valuation and they're growing really really fast.

Так на этом слайде наверное нужно разделить вот это вот про то что я могу построить свой. Нужно убрать дальше слайд там есть слайд про я экспериментировал мой бэкграунд это не нужно. Просто следующий слайд после продуктов он будет про то что давайте построим свой и это не Rocky Sense.

So the next slide is just, "Okay, well, what if I tell you that anyone can build it now?" Yes, well, what if I tell you that this is not rocket science? I mean, all the rocket science parts have already been solved for us by Frontier AI Labs. By the end of this talk, hopefully everyone will be able to build their own mini AI wipe coding agent.

ok, следующий слайд, где мы показываем компоненты этого агента, который нам нужно сделать
На самом деле я понял что тут наверное лучше разделить по другому и сказать что компоненты это а агент б это файловая система куда мы будем писать С это builder и preview как это правильно назвать? Bundler and preview? Bundle and preview? whatever and four is the feedback loop
Okay, so if we want to build an agent, what do we actually do? Let's define these components piece by piece:
1. We need the agent itself, right? This is an AI that is going to execute some actions for us.
2. We'll need an environment. We need some place to store the files. We need some way to build those files and produce some apps from our web, right?
3. We need some way to preview the result. That could be an iframe, a BDPRI frame.
4. Finally, we need some kind of way to give the agent the feedback. It needs to know if it does something weird and it has to be able to correct itself.  This is actually the key component of web coding that allows our agents to correct themselves. They can do things without our intervention, without our use of code.

All right, agents, what are they? This term is used everywhere, so it's a pretty confusing sometimes to define what it is. But the canonical definition is that it's a prompt plus tools. Let's take the example, if you take ChatGPT or any other AI, and then you ask it, "Hey, could you write a React component for me?" It will just say, "Sure, here's your code," and it will just spit out the code. And that's it, right? Okay, but what if you need to create two files? Like split the app and make multiple app files that per component right? And so you say, "Okay, write me two React components," and we'll say, "Okay, first, this is file number one, app.tsx, and take this text and insert it there." Okay, and then you as a user say, "Okay, cool, I've now finished file number one." And then it says, "Okay, well here is the contents of the next file." And then you say, "Yes, done, what do I do next?" Well, now run like a bash command and that's it.В этом слайде была диаграмма которая просто показывала чат как он идет последовательно one by one assistant и user. Получается что там это как такая как grid и в одной колонке это assistant во второй колонке user и там просто карточки появляются когда не говорят.

И дальше следующий слайд является такой же вариацией, просто в нашей обрезе его нет. Это мы сделаем. Он важен, потому что там мы показываем, как тулы даже та же самая штука она превращается в тулы. То есть, мы. Я дальше. На этом слайде говорю: "Well, actually, no one does things like that anymore. There's a nice abstraction now which is actually built-in into LLMs and it's called tool calling or function calling". So, this is essentially what it does. The only difference is that it's executed automatically. The model asks you to do certain things with certain parameters. In our case, "Hey, take this path and write this string in that file on your computer". You call this function, the actual function you do that, and then once it's done, you take the result and you pass it back to the agent. Right now, it's the same thing, so it's um, it's basically the same. And then the agent is.

И следующий слайд получается как бы завершением этого объяснения про агента и говорит о том что ну типа да вот получается что агент это типа tool и плюс системная инструкция которая и все.

Well, the next slide is about, okay, cool. Now we know what the agent is and how are we going to actually write it? Well, you don't do that manually anymore. There's a bunch of frameworks that you can now use that all have this nice agent prompt tool calling abstractions built in. There's some things that for Python like LateChain and Graph. We don't talk about that here at this JSMint app. I personally tried most of these tools, and my personal favourites are these two projects. One is Vercel EI SDK, and another one is Mastra. Mastra is in my opinion a bit more better for background agents and workflows, so for this presentation, I'm going to go with just Vercel EI SDK.They have nice support for UI, and I think that surprisingly this is one of the biggest problems when making apps. It's not the agent, it's actually the UI. Making resumability, rendering it all correctly, doing streaming and stuff like that. That's a lot of work.

Next slide is like a comparison and choosing model. Well, okay, well actually it is also quite important what model we're going to choose. Well, I think there's a website called Elam Arena and there's a leaderboard and they constantly update based on what new models are released. You there can find and then see what are models that are best at coding. One model is um I think different there is like a Frontier once by AI labs, the most recently like Claude Opus 4.5 GPT 5.2 is also good at coding and Agentic calling Gemini 3 pro, but there is also like open source models that like DeepSeek or Minimax M2. So they all have different pricing, different limitations, and capabilities, but for this particular presentation, I'm going to go with Claude Opus. It's the one that I use in Claude Code by default.

Ok. Следующий слайд это будет скорее такой микрокод-пример того как писать агенты на IA-SDK но он будет скорее как мы покажем исходник там будет просто типа агент тулы но тулы не развернуты при нажатии на тулы он уже покажет конкретный это просто такой overview для людей чтобы не понимали как это в коде выглядит
Building agents with code is a whole other topic. Just gonna give you a brief, high-level overview of what it looks like in the code. So, more or less, all frameworks follow the same approach. There is a place in your code where you define an agent, you give it a big long string with a system prompt, and then you provide a list of tools that it has access to. And then, if we expand the tool (for example, in our case, it's called "write file"), you're going to see that it's like a certain thing that requires a zote schema. That's needed because the model needs to actually understand what parameters it needs to output. And then, this is just like a normal, regular async function that does something and returns the results. That's it.

Теперь мы переходим к секции про файловую систему Environment. И тут нужно как-то переорганизовать слайды немножко, чтобы была последовательность. И еще в этот момент мы как будто бы... к этому моменту люди уже понимают, что есть Write File Tool, потому что это тот, который мы показывали на примере. И как будто бы мы можем уже не сильно разжевывать здесь этот момент. Короче, на первом слайде я говорю: "OK, well, now we have the agent. Now think about the environment. The agent actually needs to do something with the source code, right? Save it somewhere and then somehow transform it into preview - something that you will be able to see on the page. Right? So what options do we have? Well, we can store it in the memory. Not really useful. We can save it to our local disc on our machine. Yeah, that could work, and then we could use a virtual file system. It's like local, but it also gives you some kind of layers, so that it's completely isolated. It's still on your disc, but it's isolated. And then you can also actually spin up a container right? And then these containers in fact you can do in the browser, or you can do that on your local machine.


и дальше мы немного перемещаем тот слайд где мы показываем разные Software и как они как они это применяют потому что такой типа вервлю и мы его сюда перемещаем после этого и показываем вот эту таблицу с bolt-lovable троллевали.And here I say that, well, it turns out that this is one of the most important things when building for a white coding platform: "What is the environment?" Every one of these things that I mentioned uses a different approach. If you use Cursor or Claude Code, they're all local, right? They write on your local machine. That's easy. And they use the environment that you have. While a Lovable uses a combination of a container and a Supabase for backend. BOLD is quite unique because the company that did them also invented this technology called Web Containers. Web Containers is a way to spin up an iOS environment. They actually use a Docker container in your browser, so they didn't have to do it in the cloud. Replit also has containers, so it runs in the clouds. Each approach has pros and cons. Web Containers are quite limited in native code, your local machine of course gets the full capabilities, but it's also expensive for speeding up containers. Well, it's not expensive, but it's more like it requires a lot of infrastructure work. You need to take care of them. You need to make sure you kill the old ones that you don't use, et cetera. For this particular talk, we're going to go really simple. We're going to use just a local file system where our code is running on the back-end, easy.

А okay, гол на этом слайде я показываю разную скорость структуру, которую я предлагаю для этого проекта. Она отличается от того, что я с на сайте там будет. АPTSX, индекс HTML, JavaScript и может быть еще какой-то конфиг, и все. И там говорим, что вот файловая система — okay для этого, нашего wipe coding thing. Мы gonna have like a folder, that we will re-initialise with some files. So it's gonna be like um, what's the right word? Skeleton, backbone, place holder, whatever. И тогда у нас есть some files, и мы будет keep weak with our agent with tools for reading and writing files. So, the reading file will just take the path to the file and return the contents of the file, and then write will take the contents and write it.На этом же слайде мы говорим что теперь мы специально ограничили и дали агента только писать в один файл для простоты чтобы не было там не знаю соблазн что-то туда запихать мы потому что делаем очень простое приложение и нам нужно только чтобы он подписал

The next slide is, but here's the caveat. Actually, if you implement just the write file tool, it's going to be super slow because if an agent for some reason needs to rewrite the code, it will have to basically supply the entire contents of the file. Well, most of the time you don't need to edit, you just need to edit certain pieces. And how do you do that? Well, actually there is a tool that uses the approach that Claude Code uses under the hood, and that's like find and replace. So it's a tool that takes the path to the file, gets the old string and receives the new string and then just basically does replace. Really stupid, simple, but very effective. And this is what Claude Code uses.

Okay, so now I talk about bundling. Okay, so now we have the files but how do we actually display it in the browser in the preview? Well, we need to if the result is a TypeScript file, we can't just display it in the browser; we need to somehow build it and show it. There are plenty of ways to do that, but I decided to go with one because it's an all-in-one TypeScript toolkit, its own package manager. It's a super simple and fun project I'm really obsessed with it. I've been quite obsessed with it recently, and yeah, it's super cool because it has a simple API, and this is what agents love.

On this page, you need to change it so that this is index.html rather than TypeScript. It's very important that the HTML is what it contains, and the whole TypeScript is what it builds into HTML. So, this command will take the HTML file, find all imports to TypeScript, and then transform it into a JavaScript bundle, CSS, and everything. The output should be just HTML and plain CSS and JavaScript files.

Awesome. So the only piece that's missing is actually to provide the agent with the tool for bundling. We're just going to give it the tool that does bundle the calls, `bund build`, and just either returns an error or says, "Okay, the build is done."

Следующий слайд лучше сделать какой-то типа схемы такой как скелетон проекта такого типа показать что типа слева у нас какой-то там чат справа у нас iframe и там где-то еще типа API который чат вызывает и когда типа бандл происходит ну короче просто показать людям типа UI скорее чтобы они это видели как это в интерфейсе будет выглядеть

Now the next thing. The only thing that is missing is feedback. And what is that? Well it turns out that LLMs are not perfect. So they can make mistakes. They can write programmes that crash and stuff like that. Now the actually missing piece is that we need to give it some kind of feedback loop. Something that will improve. Allow the agent to actually see what went wrong. So на этом слайде нужно как-то подумать как это Пока я это все проговариваю нужно это как какую-то небольшую такую статическую штуку нарисовать. Может быть оставить как есть но не знаю.

Well, the next slide is going to be about just a preview of the list of things that I'm going to cover in this talk. This is number one, we will start with a simple one. How can we know that something went wrong?
1. We can take the iframe, we can run the page and then see console log. If something happens or if JavaScript throws, we will be able to see it in the console.
2. What if the app is working well but there is nothing on the screen or something is messed up? We need now a way to do some visual screenshots.  On this slide, there will be two options: left and right. Two options, just two cards.

Тут слайды немного перемешались и поэтому будем другую структуру использовать. После этого слайда где мы показываем 1 и 2 мы говорим ну окей давайте начнем с консулогов. И здесь я показываю просто схему используя такой же approach как мы раньше использовали где у нас там был типа LLM слева справа короче здесь мы показываем что okay how can we teach an agent to do that and let's see well let's equip it with a tool that gets console logs but here's the catch it's actually not we're not able to do that on the server we have to basically get it from the client somehow so this is a bit tricky but we will do that with so-called client tools and client side tools are tools that are not executed on your server in the agent loop but instead are proxied to the browser and then you call in the browser and then send it back. So здесь на этом слайде типа третья колонка есть где типа есть не знаю LLM сервер и дальше браузер и она идет как бы из сервера в браузер и обратно туда такая диаграмма.

And the next slide is more about screenshots now. We have this way to get what if we actually can write a tool for getting screenshots and turns out it's also possible because some of the models are multi-modal, so they can accept both texts and images and they can also get used tool calls with image output. Again, that's not every model supports it, so you have to check it beforehand, but the Claude Opus that I use supports it. What we can do is we can write a function that's called getScreenshot and getScreenshot just somehow takes a screenshot and provides the image back to the model.

На следующем слайде это просто вот это перечисление того, что можно. Это один слайд будет просто про сприншот, как их делать. И мы тут просто список сделаем, что как делать? Ну, первый вариант это типа прямо на браузере не точно и там этот HTML to Canvas вот это все. На самом деле мы будем использовать Headless Chrome по 5 ир и в моем случае я рекомендую просто браузер лишь поставить, который будет это делать. Следующий слайд, где там написано Папетир, Opens, превью, тролли-вали. Не надо, можно выпилить.

Следующий слайд Problem Console Logs Are… да-да-да это тоже не надо это всё будет на диаграммах просто показано

В следующем слайде написано Agent Picker Style, тоже удаляем, пока это будет долго, нам это пока не надо.

And the next slide is when all sections are covered, that's just we're returning and reusing the slide from the beginning where we were showing components with agent file system, building preview and feedback. We're just going to write there what exactly we use for that, just to recap. It's not just, it's going to be an agent, and we're going to say, "Okay, Cloud + agents.ai, Vercel.AI to agents.zka" and environment is like local file system + ban local file system, a built-in preview ban + unframe feedback, getting console and getting screenshots.

Okay, so the next slide is a big demo where I actually show the product in action and I say, "Okay, cool, but it sounds good in theory, but let's just see how it works." I've put together a demo. It's actually a product that I've been working on, and it's like a tool that allows you to do web coding of small real-time games or things for your presentation.

Можно оставить этот слайд про Making Agent Produce Good Design, мы его просто последуем, а я вот так быстро возьму, если нужно.

Okay, and closing thoughts. The CEO of Entropic said a few days ago that we are a year away from when models are doing most of the job of what software engineers do. He also says that most of their employees at Entropic don't write code anymore, they just build tools around them.

And that makes me think of two things:
1. In order to be efficient, we have to utilise coding agents, and this is what hopefully everyone is doing now, but I think you can be really better once you understand how it works under the hood.
2. I believe the current gap is not the models. Models are already very capable. The actual gap is tools. As we can see, the more contacts you give, the proper context engineering you do, the better they are.

And then I say, "Well actually, what it means by doing tools is... First of all, I think just choose straightforward things that LLMs are good at. Choose simple things, aim for good and simple developer experience, and also you can introduce artificial limits so that the model is more effective at what it's doing."
